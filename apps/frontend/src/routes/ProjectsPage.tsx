import { PlaceholderCard } from '../components/PlaceholderCard';

export function ProjectsPage() {
  return (
    <PlaceholderCard
      title="Projects"
      hint="Lists every project in the current session. F2 will wire this to GET /api/projects and add the create flow."
    />
  );
}